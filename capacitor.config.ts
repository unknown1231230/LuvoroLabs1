import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.goldenfalconblink',
  appName: 'golden-falcon-blink',
  webDir: 'dist'
};

export default config;
